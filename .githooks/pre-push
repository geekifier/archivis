#!/usr/bin/env bash
# Pre-push hook: run the same quality gates CI runs.
# Install: git config core.hooksPath .githooks
# Skip once: git push --no-verify
set -euo pipefail

echo "pre-push: running frontend checks…"
just check-frontend

echo "pre-push: all checks passed ✓"
