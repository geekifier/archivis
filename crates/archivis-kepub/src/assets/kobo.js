// kobo.js — vendored Kobo reader stub.
//
// Provided as a placeholder hook so KEPUB-aware reader features can attach
// when the file is loaded on a Kobo device. Kepubify ships an empty stub
// for the same reason; runtime behavior comes from the device's reader.
var kobo = window.kobo || {};
