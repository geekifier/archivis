# Pin local + CI to the same Rust release so clippy lint sets stay aligned.
# Bumping this requires running `just check` first to catch any new lints.
[toolchain]
channel = "1.95.0"
components = ["rustfmt", "clippy"]
profile = "minimal"
